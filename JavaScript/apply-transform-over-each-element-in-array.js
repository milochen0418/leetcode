/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
//https://leetcode.com/problems/apply-transform-over-each-element-in-array
var map = function(arr, fn) {
    return arr.map(fn)  
};